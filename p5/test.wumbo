bool testBool;
int a ;

struct S {
	int b;
	bool c;
	int r;
};

struct S t;

int add(int a, int b) {
	return (a+b);
}

void doNothing() {
	int r;
	bool alwaysTrue;
	bool alwaysFalse;
	alwaysTrue = true;
	r = 5;
	while (alwaysTrue) {
		repeat(r){}
	}
	r = add(r,r);
	
	if (alwaysTrue && alwaysTrue) {
		repeat(r) {
			alwaysTrue = false;
		}
	}else {
		cout << "string";
	}

	if (!alwaysFalse){}
	r = 3 + r;
	r= r * r;
	r = 3 / r;
	r = 4-r;
	t.b = 3;
	t.r = add(t.b,t.b) + 2;
	if (t.c == true) {}
	if (3 == 2) {}
	testBool = (3 < 2);
	if ( 3 > 2) {}
	if (3 >= 2) {}
	if (3 <= 2) {}
	if (3 != 2) {}
	
	cin >> r;
	cin >> alwaysTrue;
	
	cout << t.b;
	cout << t.c;
	cout << "string";
}



