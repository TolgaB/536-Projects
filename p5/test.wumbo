bool testBool;
int a ;

struct S {
	int b;
	bool c;
	int r;
};

struct S t;

int add(int a, int b) {
	int c;
	c = a + b;
	return c;
}

void doNothing() {
	int r;
	bool alwaysTrue;
	bool alwaysFalse;
	alwaysTrue = true;
	r = 5;
	while (alwaysTrue) {
		repeat(r){}
	}
	r = add(r,r);
	
	if (alwaysTrue && alwaysTrue) {
		repeat(r) {
			alwaysTrue = false;
		}
	}

	if (!alwaysFalse){}

	t.b = 3;
	t.r = add(t.b,t.b);
	if (t.c == true) {}
}



